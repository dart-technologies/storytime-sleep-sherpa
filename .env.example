# ============================================
# Storytime: Sleep Sherpa - Environment Variables
# ============================================

# IMPORTANT SECURITY NOTE
#
# This file includes BOTH:
#   1) Expo client config (safe to ship in the app bundle)
#   2) Server secrets/config for Cloud Functions (MUST NOT ship in the app bundle)
#
# Never ship API keys to the client:
#   - ELEVENLABS_API_KEY
#   - GOOGLE_CLOUD_PROJECT_ID / VERTEX_AI_LOCATION (and any service credentials)
#   - ELEVENLABS_AGENT_* (treat as server config)
#
# Set server-only values as Cloud Functions secrets / environment variables (Secret Manager),
# and keep the Expo app limited to EXPO_PUBLIC_* (plus any explicitly public URLs).

# --------------------------------------------
# Expo (Client) - Safe to ship
# --------------------------------------------

# Google OAuth Client ID (for Firebase Auth)
# Get from: https://console.cloud.google.com/apis/credentials
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# --------------------------------------------
# Firebase
# --------------------------------------------

# Firebase Web App Config
# Get from: Firebase Console > Project Settings > Your apps
EXPO_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
EXPO_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# --------------------------------------------
# Cloud Functions (Client URL)
# --------------------------------------------

# Cloud Functions Base URL (public)
# Format: https://REGION-PROJECT_ID.cloudfunctions.net
CLOUD_FUNCTIONS_URL=https://us-central1-your-project.cloudfunctions.net

# Individual Function Endpoints (optional, if using custom names)
CLOUD_FUNCTION_INTAKE=/intake
CLOUD_FUNCTION_GENERATE=/generate
CLOUD_FUNCTION_VISION=/vision
CLOUD_FUNCTION_FEATURED=/featured

# --------------------------------------------
# Analytics (Optional)
# --------------------------------------------

# Google Analytics for Firebase
EXPO_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# --------------------------------------------
# Cloud Functions (Server) - Do NOT ship
# --------------------------------------------

# Google Cloud Project ID
# Set as a Cloud Function env var / secret; do NOT include in the Expo app bundle.
# Get from: https://console.cloud.google.com/
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Vertex AI Location (e.g., us-central1)
# Set as a Cloud Function env var / secret; do NOT include in the Expo app bundle.
VERTEX_AI_LOCATION=us-central1

# ElevenLabs API Key
# Set as a Cloud Function secret; do NOT include in the Expo app bundle.
# Get from: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ElevenLabs Agent IDs (one per persona)
# Treat as server config; do NOT include in the Expo app bundle.
ELEVENLABS_AGENT_LUNA=agent_luna_id
ELEVENLABS_AGENT_KAI=agent_kai_id
ELEVENLABS_AGENT_RIVER=agent_river_id
ELEVENLABS_AGENT_ECHO=agent_echo_id
ELEVENLABS_AGENT_SAGE=agent_sage_id

# ElevenLabs Voice IDs (Text-to-Speech / pre-generated latency mask clips)
# Defaults are ASMR-style voices (from the ElevenLabs Voice Library).
# Note: your API key must have access to these voice IDs (add them to your account if needed).
# Tip: list accessible voices with: `node scripts/pregenerate-latency-masks.mjs --list-voices --env .env.local`
ELEVENLABS_VOICE_LUNA=nbk2esDn4RRk4cVDdoiE
ELEVENLABS_VOICE_KAI=KmnvDXRA0HU55Q0aqkPG
ELEVENLABS_VOICE_RIVER=TB3vNRzK8VurMejP889q
ELEVENLABS_VOICE_ECHO=Qggl4b0xRMiqOwhPtVWT
ELEVENLABS_VOICE_SAGE=nncOnHs8qpoJX0KnDisb

# ElevenLabs TTS model (optional)
ELEVENLABS_TTS_MODEL=eleven_multilingual_v2

# ElevenLabs audio output format (optional)
ELEVENLABS_OUTPUT_FORMAT=mp3_44100_128

# --------------------------------------------
# Development
# --------------------------------------------

# Environment (development, staging, production)
NODE_ENV=development

# Enable debug logging
DEBUG=true

# --------------------------------------------
# Notes
# --------------------------------------------
# 
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to version control
# 3. EXPO_PUBLIC_* variables are exposed to the client (treat them as public)
# 4. Keep API keys and provider secrets in Cloud Functions secrets/env vars
# 5. Restart the dev server after changing environment variables
#
